<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="NoticeMapper">

	<insert id="insertNotice">
		INSERT INTO NOTICE_TBL VALUES
		(SEQ_NOTICE_NO.NEXTVAL,#{noticeType},#{noticeTitle},#{noticeContent},#{noticeCreateDate},DEFAULT,DEFAULT,#{preNoticeNo},#{nextNoticeNo},#{adminNo})
	</insert>

 	<select id="selectListCount" resultType="_int">
		SELECT COUNT(*) FROM
		NOTICE_TBL WHERE IS_NOTICE_DELETED = 'N'
	</select>

	<select id="selectNoticeList" resultType="Notice">
		SELECT * FROM
		NOTICE_TBL WHERE IS_NOTICE_DELETED = 'N' ORDER BY NOTICE_NO DESC
	</select>

	<select id="selectNoticeByNo" resultType="Notice">
		SELECT * FROM
		NOTICE_TBL WHERE NOTICE_NO = #{noticeNo} AND IS_NOTICE_DELETED = 'N'
	</select>

	<update id="updateNotice">
		UPDATE NOTICE_TBL SET NOTICE_TYPE = #{noticeType},
		NOTICE_TITLE = #{noticeTitle}, NOTICE_CONTENT = #{noticeContent} WHERE
		NOTICE_NO = #{noticeNo}
	</update>
	<update id="deleteNotice">
		UPDATE NOTICE_TBL SET IS_NOTICE_DELETED = 'Y' WHERE NOTICE_NO = #{noticeNo }
	</update>
	
	<update id="updateViewCount">
		UPDATE NOTICE_TBL SET NOTICE_VIEW_COUNT = NOTICE_VIEW_COUNT +1 WHERE NOTICE_No=#{noticeNo }
	</update>
	
</mapper>